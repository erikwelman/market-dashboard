# SEC EDGAR API configuration
# Required: Your name and email for SEC EDGAR compliance (they require identification)
SEC_EDGAR_USER_AGENT="YourApp your-email@example.com"

# Market data refresh interval in milliseconds (default: 30000)
# MARKET_REFRESH_INTERVAL=30000
